import Vue from "vue";
import Vuex from 'vuex';

Vue.use(Vuex)
const state={};
const mutation={};
const actions={};
const getters={};
//用于计算属性，用户简化仓库数据
export default new Vuex.Store({
    state,
    mutation,
    actions,
    getters
})